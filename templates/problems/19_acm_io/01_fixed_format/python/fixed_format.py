import sys


def solve(input_data: str) -> str:
    """Read n and n integers from input_data, return their sum as a string.

    ACM-style: input format is:
        n
        a1 a2 ... an

    Output is the sum on its own line (trailing newline).
    """
    # Parse the input string and compute the sum.
    # Use split() to tokenize by whitespace, then int() to convert.
    #
    # 适用题型：数组求和/最大值/平均值、排序题、哈希计数、前缀和
    # 典型输入：第一行 n，第二行 n 个数
    return ""


# For actual ACM usage:
# if __name__ == "__main__":
#     data = sys.stdin.read()
#     print(solve(data), end="")
