export function solve(inputData: string): string {
    // Read n and n integers from inputData, return their sum as a string.
    // ACM-style: input format is:
    //   n
    //   a1 a2 ... an
    // Output is the sum on its own line (trailing newline).
    //
    // 适用题型：数组求和/最大值/平均值、排序题、哈希计数、前缀和
    // 典型输入：第一行 n，第二行 n 个数
    return "";
}
