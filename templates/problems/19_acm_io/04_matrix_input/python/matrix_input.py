import sys


def solve(input_data: str) -> str:
    """Read an n x m matrix and output its transpose (m x n).

    ACM-style: input format is:
        n m
        a11 a12 ... a1m
        ...
        an1 an2 ... anm

    Output is the transposed matrix: m rows of n space-separated integers,
    each row followed by a newline.
    """
    # Parse n, m, then n rows of m integers.
    # Output the transpose: m rows of n integers, space-separated.
    #
    # 适用题型：矩阵转置/旋转、二维前缀和、网格BFS/DFS、图像处理
    # 典型输入：第一行 n m，接着 n 行每行 m 个数
    return ""


# For actual ACM usage:
# if __name__ == "__main__":
#     data = sys.stdin.read()
#     print(solve(data), end="")
