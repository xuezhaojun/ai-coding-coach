export function solve(inputData: string): string {
    // Read lines until EOF. For each non-empty line, reverse the word order
    // and return it on its own line.
    // ACM-style: each line contains words separated by whitespace. Words are
    // split on any whitespace (multiple consecutive spaces collapse to a single
    // separator on output). Empty lines are skipped.
    //
    // 适用题型：字符串反转/统计/分割、密码解密、格式化输出、业务模拟
    // 典型输入：若干行字符串，读到 EOF 结束
    return "";
}
